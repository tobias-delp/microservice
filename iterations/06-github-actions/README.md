# Iteration 06: GitHub Actions

Ziel: CI und optionalen Docker-Image-Publish verstehen.

Workflows:

- `.github/workflows/ci.yml`
- `.github/workflows/docker-build.yml`

`ci.yml` installiert Dependencies, fuehrt Tests aus und baut Docker Images fuer
alle Services.

